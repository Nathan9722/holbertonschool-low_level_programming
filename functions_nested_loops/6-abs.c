#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *@n: the character to be check
 *
 * Return: If 'n' >= 0 return 'n' otherwise return '-n'.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
